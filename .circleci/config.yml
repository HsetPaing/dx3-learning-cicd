version: 2.1
jobs:
  install-node-example:
    docker:
      - image: circleci/node:14 # ジョブのコマンドが実行されるプライマリ コンテナ
    steps:
      - checkout # プロジェクト ディレクトリ内のコードをチェック アウトします
      - run : 
          name: Build front-end
          command: |
            node install --save

  test:
    docker:
      - image: circleci/node:14 # ジョブのコマンドが実行されるプライマリ コンテナ
    steps:
      - checkout # プロジェクト ディレクトリ内のコードをチェック アウトします
      - run : 
          name: Test front-end
          command: |
            npm test

workflows:
  my_workflow:
    jobs:
      - install-node-example
      - test
      ### Add the job
